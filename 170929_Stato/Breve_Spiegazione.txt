Ho utilizzato il metodo getState() della classe Thread ed ho implementato un' interfaccia che estende Runnable tramite la  
classe (Helper.java). Questa classe crea oggetti di tipo Thread che intervengono in determinati momenti nell' esecuzione 
del programma e svelano lo stato dei thread interessati.